<%= form_for(@host_execution) do |f| %>
  <% if @host_execution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@host_execution.errors.count, "error") %> prohibited this host_execution from being saved:</h2>

      <ul>
      <% @host_execution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :host %><br>
    <%= f.text_field :host %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
